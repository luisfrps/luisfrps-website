name: Deploy Geneva Relocation Dashboard to GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'geneva-relocation/**'
      - '.github/workflows/geneva-relocation-pages.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: geneva-relocation-pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload dashboard
        uses: actions/upload-pages-artifact@v4
        with:
          path: './geneva-relocation'
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
